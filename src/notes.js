// notes.js: Memuat data notes yang disimpan dalam bentuk array objek.

// 2. buat dulu array untuk menampung objek catatan pada berkas notes.js.
const notes = [];

module.exports = notes;

// 5. objek notes yang perlu kita simpan harus memiliki struktur seperti ini
/* {
 id: string,
 title: string,
 createdAt: string,
 updatedAt: string,
 tags: array of string,
 body: string,
}, */